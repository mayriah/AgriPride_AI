# Set default behavior to automatically normalize line endings
* text=auto

# Ensure Python files are treated as text
*.py text

# Ensure configuration/documentation files are text
*.txt text
*.md text
*.env* text
.gitignore text
.gitattributes text
